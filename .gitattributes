# notebook 的 diff/merge 交给 nbdime，按 cell 比较而不是按 JSON 行。
# 两边都要跑一次：pip install -r requirements.txt && nbdime config-git --enable
*.ipynb diff=jupyternotebook
*.ipynb merge=jupyternotebook

# 每跑一次就变的时间戳不入库，否则整本 notebook 永远显示"已修改"。
*.ipynb filter=nbclean
